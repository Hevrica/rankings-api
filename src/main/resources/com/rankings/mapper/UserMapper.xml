<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rankings.mapper.UserMapper">
	<insert id="create">
		insert into user(email, pwd, nickname, preference) values(#{email}, #{pwd}, #{nickname}, #{preference})
	</insert>
	<select id="read" resultType="com.rankings.domain.UserVO">
		select * from user where id = #{id}
	</select>
	<update id="update">
		update user set pwd = #{pwd}, nickname = #{nickname}, preference = #{preference} where id = #{id}
	</update>
	<delete id="delete">
		delete from user where id = #{id}
	</delete>
</mapper>