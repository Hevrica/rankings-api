<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rankings.mapper.NoticeMapper">
    <select id="getNoticeList" resultType="com.rankings.domain.NoticeVO">
 		select * from notice order by create_date desc
 	</select>
    <insert id="postNotice">
        insert into notice(title, content, writer, image, create_date)
        values (#{title}, #{content}, #{writer}, #{image}, now())
    </insert>
    <update id="updateNotice">
        <![CDATA[update notice set title=#{title}, content=#{content}, writer=#{writer}, image=#{image}, update_date=now() where id = #{id} ]]>
    </update>
    <delete id="deleteNotice">
        <![CDATA[delete from notice where id = #{id}]]>
    </delete>
</mapper>